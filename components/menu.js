import Logo from './logo';
import GithubButton from './githubButton';
import MenuButton from './menuButton';

const Menu = () => (
  <div className="menu">
    <MenuButton />
    <Logo />
    <GithubButton />
  </div>
);

export default Menu;
